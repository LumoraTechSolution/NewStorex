/**
 * @lumora/ui — design tokens and shared components.
 *
 * Import the tokens once per app:  import '@lumora/ui/tokens.css';
 *
 * Components arrive with the surfaces that need them (M1-07 terminal, M3-01 back
 * office, M4-05 console). Anything added here must honour ROADMAP §A: touch targets
 * >= 56px, tabular monospace for currency, and status colour never carrying meaning
 * without an accompanying icon and text label.
 */

export {};
