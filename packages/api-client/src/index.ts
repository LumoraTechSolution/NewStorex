/**
 * @lumora/api-client — typed client, generated from the backend's OpenAPI document.
 *
 * The terminal points this at http://127.0.0.1:8081 and the console points it at the
 * cloud. Same generated surface, different base URL: that seam is the whole reason
 * the two apps are separate (ROADMAP §B).
 *
 * Generation is wired up alongside the first real endpoint in M0-05.
 */

export {};
